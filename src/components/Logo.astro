---
import { SITE } from 'astrowind:config';

interface Props {
  /** 'header' = nav bar; 'compact' = footer / tight spaces */
  variant?: 'header' | 'compact';
}

const { variant = 'header' } = Astro.props;
const name = SITE?.name ?? 'Meridian';

const sizeClass =
  variant === 'compact'
    ? 'h-7 md:h-8 w-auto object-contain object-left'
    : 'h-10 md:h-11 w-auto object-contain object-left';
---

<img
  src="/meridian-logo.png"
  alt={`${name} — home`}
  class:list={[sizeClass, 'self-center']}
  loading="eager"
  decoding="async"
/>
