---
import Layout from '~/layouts/PageLayout.astro';

import Hero from '~/components/widgets/Hero.astro';
import Stats from '~/components/widgets/Stats.astro';
import CallToAction from '~/components/widgets/CallToAction.astro';

const metadata = {
  title: 'Meridian — The First Real-Time Psychological Stability Index for Active Traders',
  ignoreTitleTemplate: true,
};

const marqueeRow1 = [
  {
    quote: "Three months in. I thought I had clean execution — the overtrading metric disagreed from day one.",
    name: 'Jason K.',
    role: 'Futures day trader',
  },
  {
    quote: "Passed my third Apex evaluation. The first two I blew up on days where, in hindsight, I should have just closed the platform. The pattern was obvious once I could see it.",
    name: 'Mike R.',
    role: 'Prop firm trader',
  },
  {
    quote: "Turned out I wasn't losing because of my entries. I was adding to losing positions and ignoring my own rules. Took data to admit it.",
    name: 'Anonymous',
    role: '',
  },
  {
    quote: "I don't use tools that send my fill data to the cloud. Local-only was non-negotiable for me.",
    name: 'David Chen',
    role: 'Independent trader',
  },
  {
    quote: "It doesn't close your positions for you — that part is still on you. But I stopped pretending I didn't notice the score dropping while I was moving my stop around.",
    name: 'T. Rodriguez',
    role: 'NQ futures',
  },
  {
    quote: "The seven bars make it immediately clear which habit is pulling the score down. I kept telling myself I was trading fine. The data disagreed consistently.",
    name: 'Elena V.',
    role: 'MNQ scalper',
  },
  {
    quote: "Being able to use Market Replay without it touching my live baseline was more useful than I expected.",
    name: 'Chris P.',
    role: 'Futures swing',
  },
  {
    quote: "I run two accounts with copy fills. I was expecting double-counting issues in the score. It handled it cleanly without any configuration.",
    name: 'Jordan L.',
    role: 'Multi-account',
  },
  {
    quote: "After a losing session I used to review charts to explain what happened. Now I check the session report first. It's faster and more honest.",
    name: 'Sam W.',
    role: 'ES / NQ',
  },
  {
    quote: "Didn't want another subscription. Tried the trial anyway. Kept it — the overlay is actually readable mid-session without being in the way.",
    name: 'Rina M.',
    role: 'Day trader',
  },
];

const marqueeRow2 = [
  {
    quote: "The caution signal fired and I decided I could manage through it. Twenty minutes later I had hit my daily limit. I can't blame the tool — I ignored it.",
    name: 'Sara',
    role: 'ES trader',
  },
  {
    quote: "I've restarted funded accounts more times than I want to count. If someone had told me a revenge-entry detector would matter more than another indicator, I wouldn't have believed them.",
    name: 'Alex Nowak',
    role: 'Prop trader',
  },
  {
    quote: "Four years trading NQ. Thought I had a solid read on my own behavior. First week of data showed me I had several habits I wasn't aware of.",
    name: 'James O.',
    role: 'Day trader',
  },
  {
    quote: "Post-session journals have their place. This is different — the feedback arrives while the session is still running, not the next morning.",
    name: 'Anonymous',
    role: '',
  },
  {
    quote: "Took a few sessions to calibrate. Once it did, the alerts started making sense — I could trace each one back to a specific pattern.",
    name: 'Priya S.',
    role: 'Futures trader',
  },
  {
    quote: "The stop-widening flag caught me doing something I always denied — moving my stop out when a trade went against me. Useful confirmation, even if it wasn't comfortable.",
    name: 'Marcus T.',
    role: 'CL / GC',
  },
  {
    quote: "Thought my exits were disciplined. Turns out there's a consistent pattern on certain days. I don't love it, but I can work with the information.",
    name: 'Kim H.',
    role: 'Index futures',
  },
  {
    quote: "A rough Tuesday doesn't disappear from the score just because Wednesday started. That's accurate. That's how it actually feels.",
    name: 'Leo B.',
    role: 'Overnight + day',
  },
  {
    quote: "The alerts are infrequent enough that I don't tune them out. When one fires, I actually pause before doing anything.",
    name: 'Andre C.',
    role: 'Prop + personal',
  },
  {
    quote: "The history view is simple. I like that. Seeing consistent streaks makes discipline feel measurable instead of just a goal.",
    name: 'Nina F.',
    role: 'Futures',
  },
];
---

<Layout metadata={metadata}>
  <Hero
    actions={[
      {
        variant: 'primary',
        text: 'Start free trial — 7 days',
        href: 'https://meridianpsi.lemonsqueezy.com/checkout/buy/60b8b911-1d62-4f90-87a9-23f609a75c86?enabled=1539446',
        icon: 'tabler:player-play',
      },
      { text: 'See pricing', href: '/pricing' },
    ]}
  >
    <Fragment slot="title">
      Every trader has a plan.
      <br />
      <span class="text-accent dark:text-emerald-700">The market's job is to make you forget it.</span>
    </Fragment>

    <Fragment slot="subtitle">
      <strong>Meridian</strong> is the first real-time psychological stability monitor for active traders. It tracks seven behavioral patterns, learns your personal baseline, and updates your PSI score in less than 100ms during the session.
      <br /><br />
      <span class="text-zinc-500 font-normal">Most psychology tools analyze what happened. Meridian tells you what's happening — <em>right now</em>.</span>
    </Fragment>
  </Hero>

  <Stats
    stats={[
      { title: 'Live behavioral signals', amount: '7' },
      { title: 'Updated after and during session', amount: '<100ms' },
      { title: 'Session history', amount: '5 years' },
      { title: 'Free trial, no charge', amount: '7-day' },
    ]}
  />

  <!-- Problem recognition section — institutional vs retail framing -->
  <section class="py-16 px-6 bg-zinc-50/60">
    <div class="max-w-4xl mx-auto text-center">
      <h2 class="text-3xl md:text-4xl font-bold text-zinc-900 mb-4">
        Institutional traders have risk managers.<br />You have yourself.
      </h2>
      <p class="text-lg text-zinc-600 mb-12 max-w-2xl mx-auto">
        They have compliance officers, position limits, and systems that enforce rules outside their control. Retail traders have experience, hard-won rules — and then the moment everything goes wrong, nothing stops them from breaking all of them.
      </p>
      <ul class="text-left max-w-lg mx-auto space-y-4 mb-12">
        {[
          'Added size right after a loss',
          'Moved the stop to "give it a little more room"',
          'Took the next trade too fast — still feeling the last one',
          'Held a loser longer than your plan ever allowed',
          'Broke your session rule — just this once',
          'Hit your daily limit and took one more anyway',
        ].map((item) => (
          <li class="flex items-start gap-3 text-base text-zinc-700">
            <span class="mt-2 h-2 w-2 rounded-full bg-red-400 shrink-0"></span>
            {item}
          </li>
        ))}
      </ul>
      <p class="text-lg font-semibold text-zinc-900 max-w-xl mx-auto">
        Meridian doesn't tell you what to trade.<br />
        It tells you <em>where you are</em> — so you can decide whether to continue.
      </p>
    </div>
  </section>

  <!-- Live score — dark showcase -->
  <section class="relative py-28 px-6 overflow-hidden">
    <!-- Gradient fade from white above into dark, and back to white below -->
    <div class="pointer-events-none absolute inset-0 bg-[#0a0a0a]"></div>

    <!-- Red ambient glow — matches the screenshot's critical alarm color -->
    <div class="pointer-events-none absolute top-1/2 left-1/4 -translate-x-1/2 -translate-y-1/2 w-[500px] h-[500px] rounded-full bg-red-700/10 blur-[100px]"></div>
    <!-- Subtle green counter-glow on the right for the green toggle -->
    <div class="pointer-events-none absolute top-1/2 right-1/4 translate-x-1/2 -translate-y-1/2 w-[300px] h-[300px] rounded-full bg-emerald-700/8 blur-[80px]"></div>

    <div class="relative z-20 max-w-6xl mx-auto grid md:grid-cols-[55fr_45fr] gap-12 md:gap-20 items-center">
      <!-- Screenshot — larger, floats with depth shadow -->
      <div class="order-1 md:order-1">
        <div class="rounded-2xl overflow-hidden shadow-[0_40px_100px_rgba(0,0,0,0.8),0_0_0_1px_rgba(255,255,255,0.06)]">
          <img
            src="/screenshots/psi-critical.png"
            alt="Meridian PSI Monitor showing a Critical score of 47 with behavioral signal bars"
            class="w-full h-auto block"
            loading="eager"
          />
        </div>
      </div>
      <!-- Text (white on dark) -->
      <div class="order-2 md:order-2">
        <p class="text-xs font-semibold tracking-widest text-zinc-400 uppercase mb-3">Live session view</p>
        <h2 class="text-3xl md:text-4xl font-bold text-white leading-snug mb-5">
          A live view of your trading stability.
        </h2>
        <p class="text-zinc-400 leading-relaxed text-base mb-6">
          Your PSI updates in real time during the session, with clear signals showing what is driving the score.
        </p>
        <ul class="space-y-3 text-sm text-zinc-300">
          {['Updates during the session, not just after fills', 'Seven visible signal bars show what is driving the score', 'Session stats: trades, win rate, alerts, duration', 'PSI timeline shows how pressure evolved across the session'].map(item => (
            <li class="flex items-start gap-2.5">
              <span class="mt-1 h-1.5 w-1.5 rounded-full bg-zinc-500 shrink-0"></span>
              {item}
            </li>
          ))}
        </ul>
        <div class="mt-8">
          <a href="/features" class="inline-flex items-center gap-1.5 text-sm font-medium text-white/75 hover:text-white transition-colors">
            See full feature breakdown &rarr;
          </a>
        </div>
      </div>
    </div>
  </section>

  <!-- Section 2: Seven dimensions — CSS visual layout, no screenshot needed -->
  <section class="py-16 px-6 bg-zinc-50/60 border-t border-zinc-100">
    <div class="max-w-5xl mx-auto">
      <div class="mb-8">
        <p class="text-xs font-semibold tracking-widest text-emerald-700 uppercase mb-2">Behavioral signals</p>
        <h2 class="text-2xl md:text-3xl font-bold text-zinc-900 leading-snug mb-3">
          Seven patterns. Each one specific to a known failure mode.
        </h2>
        <p class="text-zinc-500 leading-relaxed text-sm max-w-2xl">
          Every signal tracks a behavioral pattern from behavioral-finance research. They run independently and combine into a single PSI score.
        </p>
      </div>
      <div class="space-y-2">
        {[
          { name: 'Revenge Entry',     desc: 'Re-entry after loss with elevated size',              pct: 82 },
          { name: 'Stop Manipulation', desc: 'Widening stops in the adverse direction',             pct: 55 },
          { name: 'Size Spike',        desc: 'Position exceeds your declared rule',                 pct: 95 },
          { name: 'Hold Bias',         desc: 'Abnormal hold duration vs your own baseline',        pct: 38 },
          { name: 'Position Overstay', desc: 'Holding a loser past your 90th-percentile hold time', pct: 60 },
          { name: 'Rule Violations',   desc: 'Trading outside your declared session rules',         pct: 44 },
          { name: 'Overtrading Pace',  desc: 'Entry frequency accelerating vs your history',        pct: 71 },
        ].map(({ name, desc, pct }) => (
          <div class="flex items-center gap-4 bg-white rounded-lg border border-zinc-100 px-5 py-2.5 shadow-sm">
            <span class="w-36 shrink-0 text-sm font-semibold text-zinc-900">{name}</span>
            <div class="flex-1 h-1.5 bg-zinc-100 rounded-full overflow-hidden">
              <div class="h-full rounded-full bg-gradient-to-r from-emerald-500 to-emerald-400" style={`width:${pct}%`}></div>
            </div>
            <span class="w-56 shrink-0 text-xs text-zinc-400 text-right hidden sm:block">{desc}</span>
          </div>
        ))}
      </div>
      <div class="mt-5">
        <a href="/features#seven-dimensions" class="inline-flex items-center gap-1.5 text-sm font-medium text-emerald-700 hover:text-emerald-900 transition-colors">
          Read the technical detail &rarr;
        </a>
      </div>
    </div>
  </section>

  <!-- Section 3: Session history — left carousel, right text -->
  <section class="py-20 lg:py-28 px-6 bg-white border-t border-zinc-100">
    <div class="max-w-6xl mx-auto grid md:grid-cols-2 gap-12 md:gap-20 items-center">
      <!-- Left: image carousel -->
      <div class="relative group/carousel">
        <!-- Slide 1: December -->
        <div data-carousel="history">
          <div class="rounded-2xl overflow-hidden shadow-xl border border-zinc-800">
            <img
              src="/screenshots/session-log-december.png"
              alt="Session log December 2025"
              class="w-full max-h-[380px] object-cover object-top block"
              loading="lazy"
            />
          </div>
          <div class="mt-3 flex items-center flex-wrap gap-2 px-1">
            <span class="bg-red-500 text-white text-xs font-bold px-3 py-1 rounded-full">Month 1 — December 2025</span>
            <span class="text-red-500 font-semibold text-sm">Composure 49%</span>
            <span class="text-zinc-400 text-sm">·</span>
            <span class="text-red-500 font-semibold text-sm">Avg P&amp;L &minus;$629</span>
          </div>
        </div>
        <!-- Slide 2: April -->
        <div data-carousel="history" class="hidden">
          <div class="rounded-2xl overflow-hidden shadow-xl border border-zinc-800">
            <img
              src="/screenshots/session-log-april.png"
              alt="Session log April 2026"
              class="w-full max-h-[380px] object-cover object-top block"
              loading="lazy"
            />
          </div>
          <div class="mt-3 flex items-center flex-wrap gap-2 px-1">
            <span class="bg-emerald-500 text-white text-xs font-bold px-3 py-1 rounded-full">Month 5 — April 2026</span>
            <span class="text-emerald-600 font-semibold text-sm">Composure 79%</span>
            <span class="text-zinc-400 text-sm">·</span>
            <span class="text-emerald-600 font-semibold text-sm">Avg P&amp;L +$997</span>
          </div>
        </div>

        <!-- Overlay prev arrow — outside overflow-hidden -->
        <button
          data-prev="history"
          class="absolute left-3 top-[46%] -translate-y-1/2 z-10 h-9 w-9 rounded-full bg-black/30 hover:bg-black/60 backdrop-blur-sm text-white flex items-center justify-center transition-all opacity-0 group-hover/carousel:opacity-100 focus-visible:opacity-100"
          aria-label="Previous"
        >
          <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor" class="w-4 h-4"><path fill-rule="evenodd" d="M11.78 5.22a.75.75 0 0 1 0 1.06L8.06 10l3.72 3.72a.75.75 0 1 1-1.06 1.06l-4.25-4.25a.75.75 0 0 1 0-1.06l4.25-4.25a.75.75 0 0 1 1.06 0Z" clip-rule="evenodd" /></svg>
        </button>

        <!-- Overlay next arrow -->
        <button
          data-next="history"
          class="absolute right-3 top-[46%] -translate-y-1/2 z-10 h-9 w-9 rounded-full bg-black/30 hover:bg-black/60 backdrop-blur-sm text-white flex items-center justify-center transition-all opacity-0 group-hover/carousel:opacity-100 focus-visible:opacity-100"
          aria-label="Next"
        >
          <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor" class="w-4 h-4"><path fill-rule="evenodd" d="M8.22 5.22a.75.75 0 0 1 1.06 0l4.25 4.25a.75.75 0 0 1 0 1.06l-4.25 4.25a.75.75 0 1 1-1.06-1.06L11.94 10 8.22 6.28a.75.75 0 0 1 0-1.06Z" clip-rule="evenodd" /></svg>
        </button>

        <!-- Dot indicators -->
        <div class="absolute bottom-[52px] left-1/2 -translate-x-1/2 z-10 flex gap-1.5">
          <button data-dot="history" class="h-1.5 w-4 rounded-full bg-white/90 transition-all" aria-label="Slide 1"></button>
          <button data-dot="history" class="h-1.5 w-1.5 rounded-full bg-white/40 hover:bg-white/70 transition-all" aria-label="Slide 2"></button>
        </div>
      </div>
      <!-- Right: text -->
      <div>
        <p class="text-xs font-semibold tracking-widest text-emerald-700 uppercase mb-3">Session review and history</p>
        <h2 class="text-3xl md:text-4xl font-bold text-zinc-900 leading-snug mb-5">
          Not just P&amp;L. Whether you traded right.
        </h2>
        <p class="text-zinc-500 leading-relaxed text-base mb-6">
          Meridian tracks discipline session by session. Over time, the pattern becomes visible — and so does the improvement.
        </p>
        <ul class="space-y-3 text-sm text-zinc-700">
          {[
            'Every session saved with PSI score, Composure, and trade stats',
            'Month-over-month behavioral progress visible at a glance',
            'Separate from P&L — shows whether you traded in control',
            '5 years of session history with Meridian Guard',
          ].map(item => (
            <li class="flex items-start gap-2.5">
              <span class="mt-1 h-1.5 w-1.5 rounded-full bg-emerald-500 shrink-0"></span>
              {item}
            </li>
          ))}
        </ul>
      </div>
    </div>
  </section>

  <!-- Section: Intel Layer — feature showcase -->
  <section class="py-20 lg:py-28 px-6 bg-zinc-50/60 border-t border-zinc-100">
    <div class="max-w-6xl mx-auto grid md:grid-cols-[2fr_3fr] gap-14 md:gap-20 items-center">

      <!-- Left: copy -->
      <div>
        <p class="text-xs font-semibold tracking-widest text-emerald-700 uppercase mb-3">Exclusive analysis · From day to year</p>
        <h2 class="text-3xl md:text-4xl font-bold text-zinc-900 leading-snug mb-5">
          Data no other trading journal can show you.
        </h2>
        <p class="text-zinc-500 text-base leading-relaxed mb-6">
          No trade journal, no broker dashboard, no analytics platform cross-references your psychological state with your P&amp;L across five years of sessions. Meridian does — and it's the only one that does.
        </p>
        <ul class="space-y-3 text-sm text-zinc-600">
          {[
            'Monthly behavioral grade — not just P&L, but how you traded it',
            'PSI × P&L correlation mapped across your entire history',
            'Weakest-day patterns and 6-month Composure trend',
            "Today's Risk Brief — built from your own data, every morning",
          ].map(item => (
            <li class="flex items-start gap-2.5">
              <span class="mt-1 h-1.5 w-1.5 rounded-full bg-emerald-500 shrink-0"></span>
              {item}
            </li>
          ))}
        </ul>
        <div class="mt-8">
          <a href="/guard" class="inline-flex items-center gap-1.5 text-sm font-medium text-emerald-700 hover:text-emerald-900 transition-colors">
            Explore all features &rarr;
          </a>
        </div>
      </div>

      <!-- Right: image switcher -->
      <div>
        <!-- Slide label -->
        <p data-label-display="intel-features" class="text-xs font-medium text-zinc-400 text-center mb-3 min-h-[1rem]">
          Complete session log — daily PSI, Composure and P&amp;L all in one view
        </p>

        <!-- Arrows sit outside the image using px padding + absolute positioning -->
        <div class="relative px-11">
          <!-- Image slides — show/hide so each image retains its natural proportions -->
          <div class="rounded-xl shadow-md overflow-hidden">
            <div data-slide="intel-features" data-slide-label="Complete session log — daily PSI, Composure and P&L all in one view">
              <img src="/screenshots/intel-session-calendar.png" alt="Session log calendar" class="w-full h-auto block" loading="lazy" />
            </div>
            <div data-slide="intel-features" class="hidden" data-slide-label="Monthly digest — behavioral grade, trend, and best / worst sessions">
              <img src="/screenshots/intel-monthly-digest.png" alt="Monthly digest" class="w-full h-auto block" loading="lazy" />
            </div>
            <div data-slide="intel-features" class="hidden" data-slide-label="Today's Risk Brief — personalized pre-session intelligence, every morning">
              <img src="/screenshots/intel-risk-brief.png" alt="Today's Risk Brief" class="w-full h-auto block" loading="lazy" />
            </div>
            <div data-slide="intel-features" class="hidden" data-slide-label="PSI × P&L correlation — from detailed pattern to regression analysis">
              <img src="/screenshots/intel-psi-performance.png" alt="PSI x Performance correlation" class="w-full h-auto block" loading="lazy" />
            </div>
            <div data-slide="intel-features" class="hidden" data-slide-label="Six-month Composure trend — long-term behavioral improvement made visible">
              <img src="/screenshots/intel-composure-progress.png" alt="Composure progress 6 months" class="w-full h-auto block" loading="lazy" />
            </div>
          </div>

          <!-- Prev arrow — absolute, left of image -->
          <button
            data-prev="intel-features"
            class="absolute left-0 top-1/2 -translate-y-1/2 w-9 h-9 rounded-full border-2 border-emerald-600 text-emerald-600 hover:bg-emerald-600 hover:text-white bg-white flex items-center justify-center transition-all duration-200 shadow-sm"
            aria-label="Previous"
          >
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor" class="w-4 h-4">
              <path fill-rule="evenodd" d="M11.78 5.22a.75.75 0 0 1 0 1.06L8.06 10l3.72 3.72a.75.75 0 1 1-1.06 1.06l-4.25-4.25a.75.75 0 0 1 0-1.06l4.25-4.25a.75.75 0 0 1 1.06 0Z" clip-rule="evenodd"/>
            </svg>
          </button>

          <!-- Next arrow — absolute, right of image -->
          <button
            data-next="intel-features"
            class="absolute right-0 top-1/2 -translate-y-1/2 w-9 h-9 rounded-full border-2 border-emerald-600 text-emerald-600 hover:bg-emerald-600 hover:text-white bg-white flex items-center justify-center transition-all duration-200 shadow-sm"
            aria-label="Next"
          >
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor" class="w-4 h-4">
              <path fill-rule="evenodd" d="M8.22 5.22a.75.75 0 0 1 1.06 0l4.25 4.25a.75.75 0 0 1 0 1.06l-4.25 4.25a.75.75 0 1 1-1.06-1.06L11.94 10 8.22 6.28a.75.75 0 0 1 0-1.06Z" clip-rule="evenodd"/>
            </svg>
          </button>
        </div>

        <!-- Progress dots -->
        <div class="flex justify-center gap-1.5 mt-4">
          <button data-dot="intel-features" class="h-1.5 w-4 rounded-full bg-emerald-500 transition-all" aria-label="Slide 1"></button>
          <button data-dot="intel-features" class="h-1.5 w-1.5 rounded-full bg-zinc-300 hover:bg-zinc-400 transition-all" aria-label="Slide 2"></button>
          <button data-dot="intel-features" class="h-1.5 w-1.5 rounded-full bg-zinc-300 hover:bg-zinc-400 transition-all" aria-label="Slide 3"></button>
          <button data-dot="intel-features" class="h-1.5 w-1.5 rounded-full bg-zinc-300 hover:bg-zinc-400 transition-all" aria-label="Slide 4"></button>
          <button data-dot="intel-features" class="h-1.5 w-1.5 rounded-full bg-zinc-300 hover:bg-zinc-400 transition-all" aria-label="Slide 5"></button>
        </div>
      </div>

    </div>
  </section>

  <!-- Section 4: Personalized baselines -->
  <section class="py-20 lg:py-28 px-6 bg-zinc-50/60 border-t border-zinc-100">
    <div class="max-w-6xl mx-auto grid md:grid-cols-[3fr_2fr] gap-12 md:gap-20 items-center">
      <!-- Text -->
      <div class="order-2 md:order-1">
        <p class="text-xs font-semibold tracking-widest text-emerald-700 uppercase mb-3">Personalized baselines</p>
        <h2 class="text-3xl md:text-4xl font-bold text-zinc-900 leading-snug mb-5">
          Built around your behavior. Not a generic threshold.
        </h2>
        <p class="text-zinc-600 leading-relaxed text-base mb-6">
          Meridian learns what normal looks like for you, then makes drift visible when your behavior starts to change. The PSI score is built around your own history, so it stays useful across different trading styles instead of relying on a one-size-fits-all warning level.
        </p>
        <ul class="space-y-3 text-sm text-zinc-700">
          {[
            'Built from your own trading history',
            'Personal baseline instead of a generic rule',
            'Detailed profile settings for different styles, rules, and workflows',
            'Becomes more useful as more sessions are recorded',
          ].map(item => (
            <li class="flex items-start gap-2.5">
              <span class="mt-1 h-1.5 w-1.5 rounded-full bg-emerald-500 shrink-0"></span>
              {item}
            </li>
          ))}
        </ul>
        <div class="mt-8">
          <a href="/features" class="inline-flex items-center gap-1.5 text-sm font-medium text-emerald-700 hover:text-emerald-900 transition-colors">
            Full technical breakdown &rarr;
          </a>
        </div>
      </div>
      <!-- Baseline screenshot -->
      <div class="order-1 md:order-2">
        <div class="rounded-2xl overflow-hidden shadow-xl border border-zinc-800">
          <img
            src="/screenshots/baseline-learning.png"
            alt="Meridian baseline learning panel showing personal trading statistics"
            class="w-full h-auto block"
            loading="lazy"
          />
        </div>
      </div>
    </div>
  </section>

  <!-- Guard teaser -->
  <section class="py-20 lg:py-28 px-6 bg-zinc-900 text-white">
    <div class="max-w-5xl mx-auto">
      <div class="text-center mb-12">
        <p class="text-xs font-semibold tracking-[0.15em] text-emerald-400 uppercase mb-4">Meridian Guard — Coming soon</p>
        <h2 class="text-3xl md:text-4xl font-bold mb-4">
          You already know the rules.<br />Guard makes sure you follow them.
        </h2>
        <p class="text-zinc-400 text-base max-w-2xl mx-auto leading-relaxed">
          When you are calm, you write the rules. When the moment comes, Guard enforces them — automatically, without requiring a decision from you. This is what behavioral economists call a commitment device.
        </p>
      </div>

      <!-- 3 key feature callouts -->
      <div class="grid sm:grid-cols-3 gap-4 mb-10">
        <div class="bg-zinc-800/60 border border-zinc-700/50 rounded-2xl p-5">
          <p class="text-emerald-400 text-xs font-bold tracking-widest uppercase mb-2">Guard System</p>
          <p class="text-white font-semibold mb-2">Rules that enforce themselves</p>
          <p class="text-zinc-400 text-sm leading-relaxed">6 trigger conditions. 5 action levels from a quiet toast to a full broker disconnect. Set once — active every session.</p>
        </div>
        <div class="bg-zinc-800/60 border border-zinc-700/50 rounded-2xl p-5">
          <p class="text-emerald-400 text-xs font-bold tracking-widest uppercase mb-2">Intel Layer</p>
          <p class="text-white font-semibold mb-2">See patterns across months</p>
          <p class="text-zinc-400 text-sm leading-relaxed">Monthly digest, PSI &times; P&amp;L correlation, weekday patterns, and a pre-session risk brief — built from your own history.</p>
        </div>
        <div class="bg-zinc-800/60 border border-zinc-700/50 rounded-2xl p-5">
          <p class="text-emerald-400 text-xs font-bold tracking-widest uppercase mb-2">Key finding</p>
          <p class="text-white font-semibold mb-2">Stable PSI &rarr; +$1,402/session</p>
          <p class="text-zinc-400 text-sm leading-relaxed">Critical PSI &rarr; &minus;$550/session. Guard shows you — and stops you — before you get there.</p>
        </div>
      </div>

      <div class="text-center">
        <a
          href="/guard"
          class="inline-flex items-center gap-2 bg-emerald-600 hover:bg-emerald-500 text-white font-semibold text-sm rounded-full px-8 py-3 transition-colors duration-200"
        >
          See everything in Meridian Guard
          <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
          </svg>
        </a>
      </div>
    </div>
  </section>

  <!-- Testimonials — dual-row infinite marquee -->
  <section class="py-12 bg-zinc-50 border-t border-zinc-100">
    <div class="max-w-5xl mx-auto px-6 mb-8 text-center">
      <p class="text-xs font-semibold tracking-widest text-emerald-700 uppercase mb-2">What traders say</p>
      <h2 class="text-2xl md:text-3xl font-bold text-zinc-900">From traders who've been there</h2>
    </div>

    <div
      class="overflow-hidden"
      style="mask-image: linear-gradient(to right, transparent, black 12%, black 88%, transparent); -webkit-mask-image: linear-gradient(to right, transparent, black 12%, black 88%, transparent);"
    >
      <!-- Row 1: scroll left — two strips, no gap between strips, so -50% == one full cycle -->
      <div class="marquee-left flex w-max mb-3 will-change-transform">
        <div class="flex gap-3 shrink-0">
          {marqueeRow1.map((t) => (
            <div class="w-60 shrink-0 bg-white rounded-xl border border-zinc-200 px-4 py-3.5 shadow-sm">
              <div class="flex items-center justify-between gap-2 mb-2">
                <div class="flex items-center gap-2">
                  <div class="w-6 h-6 rounded-full bg-emerald-100 text-emerald-700 text-xs font-bold flex items-center justify-center shrink-0 select-none">
                    {t.name === 'Anonymous' ? '?' : t.name.charAt(0)}
                  </div>
                  <div>
                    <p class="font-semibold text-zinc-900 text-xs leading-tight">{t.name}</p>
                    {t.role && <p class="text-xs text-zinc-400">{t.role}</p>}
                  </div>
                </div>
                <div class="flex gap-0.5 shrink-0">
                  {Array(5).fill(null).map(() => (
                    <svg class="w-2.5 h-2.5 text-amber-400" fill="currentColor" viewBox="0 0 20 20">
                      <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z" />
                    </svg>
                  ))}
                </div>
              </div>
              <p class="text-zinc-600 text-xs leading-relaxed">"{t.quote}"</p>
            </div>
          ))}
        </div>
        <div class="flex gap-3 shrink-0" aria-hidden="true">
          {marqueeRow1.map((t) => (
            <div class="w-60 shrink-0 bg-white rounded-xl border border-zinc-200 px-4 py-3.5 shadow-sm">
              <div class="flex items-center justify-between gap-2 mb-2">
                <div class="flex items-center gap-2">
                  <div class="w-6 h-6 rounded-full bg-emerald-100 text-emerald-700 text-xs font-bold flex items-center justify-center shrink-0 select-none">
                    {t.name === 'Anonymous' ? '?' : t.name.charAt(0)}
                  </div>
                  <div>
                    <p class="font-semibold text-zinc-900 text-xs leading-tight">{t.name}</p>
                    {t.role && <p class="text-xs text-zinc-400">{t.role}</p>}
                  </div>
                </div>
                <div class="flex gap-0.5 shrink-0">
                  {Array(5).fill(null).map(() => (
                    <svg class="w-2.5 h-2.5 text-amber-400" fill="currentColor" viewBox="0 0 20 20">
                      <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z" />
                    </svg>
                  ))}
                </div>
              </div>
              <p class="text-zinc-600 text-xs leading-relaxed">"{t.quote}"</p>
            </div>
          ))}
        </div>
      </div>

      <!-- Row 2: scroll right -->
      <div class="marquee-right flex w-max will-change-transform">
        <div class="flex gap-3 shrink-0">
          {marqueeRow2.map((t) => (
            <div class="w-60 shrink-0 bg-white rounded-xl border border-zinc-200 px-4 py-3.5 shadow-sm">
              <div class="flex items-center justify-between gap-2 mb-2">
                <div class="flex items-center gap-2">
                  <div class="w-6 h-6 rounded-full bg-emerald-100 text-emerald-700 text-xs font-bold flex items-center justify-center shrink-0 select-none">
                    {t.name === 'Anonymous' ? '?' : t.name.charAt(0)}
                  </div>
                  <div>
                    <p class="font-semibold text-zinc-900 text-xs leading-tight">{t.name}</p>
                    {t.role && <p class="text-xs text-zinc-400">{t.role}</p>}
                  </div>
                </div>
                <div class="flex gap-0.5 shrink-0">
                  {Array(5).fill(null).map(() => (
                    <svg class="w-2.5 h-2.5 text-amber-400" fill="currentColor" viewBox="0 0 20 20">
                      <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z" />
                    </svg>
                  ))}
                </div>
              </div>
              <p class="text-zinc-600 text-xs leading-relaxed">"{t.quote}"</p>
            </div>
          ))}
        </div>
        <div class="flex gap-3 shrink-0" aria-hidden="true">
          {marqueeRow2.map((t) => (
            <div class="w-60 shrink-0 bg-white rounded-xl border border-zinc-200 px-4 py-3.5 shadow-sm">
              <div class="flex items-center justify-between gap-2 mb-2">
                <div class="flex items-center gap-2">
                  <div class="w-6 h-6 rounded-full bg-emerald-100 text-emerald-700 text-xs font-bold flex items-center justify-center shrink-0 select-none">
                    {t.name === 'Anonymous' ? '?' : t.name.charAt(0)}
                  </div>
                  <div>
                    <p class="font-semibold text-zinc-900 text-xs leading-tight">{t.name}</p>
                    {t.role && <p class="text-xs text-zinc-400">{t.role}</p>}
                  </div>
                </div>
                <div class="flex gap-0.5 shrink-0">
                  {Array(5).fill(null).map(() => (
                    <svg class="w-2.5 h-2.5 text-amber-400" fill="currentColor" viewBox="0 0 20 20">
                      <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z" />
                    </svg>
                  ))}
                </div>
              </div>
              <p class="text-zinc-600 text-xs leading-relaxed">"{t.quote}"</p>
            </div>
          ))}
        </div>
      </div>
    </div>

    <p class="text-center text-xs text-zinc-400 mt-6 italic">Results may vary. Trading involves substantial risk of loss.</p>
  </section>

  <!-- Founder note -->
  <section class="py-14 px-6 bg-white border-t border-zinc-100">
    <div class="max-w-2xl mx-auto flex gap-6 items-start">
      <div class="shrink-0 w-10 h-10 rounded-full bg-emerald-100 text-emerald-700 text-sm font-bold flex items-center justify-center select-none">G</div>
      <div>
        <p class="text-zinc-700 leading-relaxed text-base italic">
          "I built Meridian because I kept blowing up sessions that had no reason to go wrong. The strategy was solid. The market wasn't the problem. I was. I needed something that told me that <em>while it was happening</em> — not the next morning in a journal."
        </p>
        <p class="mt-3 text-sm text-zinc-400 font-medium">— Gary Caffrey, Founder of Meridian &nbsp;·&nbsp; <a href="/about" class="underline hover:text-zinc-600">Read the full story →</a></p>
      </div>
    </div>
  </section>

  <section class="py-10 px-6 bg-zinc-50/80 border-t border-zinc-100">
    <div class="max-w-2xl mx-auto">
      <p class="text-xs text-zinc-500 leading-relaxed text-center">
        PSI is a behavioral proxy index derived from your trading activity inside NinjaTrader. It does not measure physiology (for example heart rate or stress hormones), and it is not validated as a predictor of trading profits or losses. Meridian is designed to improve self-awareness and discipline during a session; outcomes still depend on many factors beyond psychology.
      </p>
    </div>
  </section>

  <CallToAction
    actions={[
      {
        variant: 'primary',
        text: 'Start free trial — 7 days',
        href: 'https://meridianpsi.lemonsqueezy.com/checkout/buy/60b8b911-1d62-4f90-87a9-23f609a75c86?enabled=1539446',
        icon: 'tabler:player-play',
      },
      {
        text: 'See pricing',
        href: '/pricing',
      },
    ]}
  >
    <Fragment slot="title">Every session starts with the best intentions.</Fragment>

    <Fragment slot="subtitle">
      Meridian helps you keep them.
      <br />
      <span class="text-sm text-muted italic mt-2 block">
        $24.99/month after 7-day trial · cancel any time · NinjaTrader 8 required.
        Trading involves substantial risk of loss. Meridian does not guarantee trading results.
      </span>
    </Fragment>
  </CallToAction>
</Layout>

<style>
  .marquee-left {
    animation: marquee-left 130s linear infinite;
  }
  .marquee-right {
    animation: marquee-right 130s linear infinite;
  }
  @keyframes marquee-left {
    from { transform: translateX(0); }
    to   { transform: translateX(-50%); }
  }
  @keyframes marquee-right {
    from { transform: translateX(-50%); }
    to   { transform: translateX(0); }
  }
  .marquee-left:hover,
  .marquee-right:hover {
    animation-play-state: paused;
  }
</style>

<script>
  function initCarousel(id: string) {
    const slides = Array.from(document.querySelectorAll<HTMLElement>(`[data-carousel="${id}"]`));
    const dots   = Array.from(document.querySelectorAll<HTMLElement>(`[data-dot="${id}"]`));
    if (!slides.length) return;
    let current = 0;

    function goTo(index: number) {
      slides[current].classList.add('hidden');
      dots[current]?.classList.remove('bg-white/90', 'w-4');
      dots[current]?.classList.add('bg-white/40', 'w-1.5');
      current = ((index % slides.length) + slides.length) % slides.length;
      slides[current].classList.remove('hidden');
      dots[current]?.classList.add('bg-white/90', 'w-4');
      dots[current]?.classList.remove('bg-white/40', 'w-1.5');
    }

    document.querySelector(`[data-prev="${id}"]`)?.addEventListener('click', () => goTo(current - 1));
    document.querySelector(`[data-next="${id}"]`)?.addEventListener('click', () => goTo(current + 1));
    dots.forEach((dot, i) => dot.addEventListener('click', () => goTo(i)));
  }

  function initSlider(id: string) {
    const slides = Array.from(document.querySelectorAll<HTMLElement>(`[data-slide="${id}"]`));
    if (!slides.length) return;
    const dots = Array.from(document.querySelectorAll<HTMLElement>(`[data-dot="${id}"]`));
    const labelEl = document.querySelector<HTMLElement>(`[data-label-display="${id}"]`);
    let current = 0;

    function goTo(n: number) {
      const prev = current;
      current = ((n % slides.length) + slides.length) % slides.length;
      slides[prev].classList.add('hidden');
      slides[current].classList.remove('hidden');
      dots[prev]?.classList.remove('w-4', 'bg-emerald-500');
      dots[prev]?.classList.add('w-1.5', 'bg-zinc-300');
      dots[current]?.classList.add('w-4', 'bg-emerald-500');
      dots[current]?.classList.remove('w-1.5', 'bg-zinc-300');
      if (labelEl) {
        labelEl.textContent = slides[current].dataset.slideLabel ?? '';
      }
    }

    document.querySelector(`[data-prev="${id}"]`)?.addEventListener('click', () => goTo(current - 1));
    document.querySelector(`[data-next="${id}"]`)?.addEventListener('click', () => goTo(current + 1));
    dots.forEach((dot, i) => dot.addEventListener('click', () => goTo(i)));
  }

  function boot() {
    initCarousel('history');
    initSlider('intel-features');
  }

  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', boot);
  } else {
    boot();
  }
</script>
