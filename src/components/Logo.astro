---
import { SITE } from 'astrowind:config';

interface Props {
  /** 'header' = nav bar; 'compact' = footer / tight spaces */
  variant?: 'header' | 'compact';
}

const { variant = 'header' } = Astro.props;
const name = SITE?.name ?? 'Meridian';

const sizeClass =
  variant === 'compact'
    ? 'h-6 md:h-7 w-auto max-w-[180px] object-contain object-left'
    : 'h-8 md:h-9 w-auto max-w-[200px] sm:max-w-none object-contain object-left';
---

<img
  src="/meridian-logo.png"
  alt={`${name} — home`}
  class:list={[sizeClass, 'self-center']}
  width="220"
  height="48"
  loading="eager"
  decoding="async"
/>
